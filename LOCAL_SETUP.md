# 💻 Запуск сайта локально (на вашем компьютере)

Инструкция по запуску и тестированию сайта БЕЗ хостинга - полностью бесплатно!

## 📋 Что нужно установить

### 1. Python 3.11+

**Проверить есть ли Python:**
```powershell
python --version
```

Если нет или версия < 3.11:
1. Скачать с https://www.python.org/downloads/
2. Установить (ВАЖНО: отметить "Add Python to PATH")
3. Перезапустить PowerShell

### 2. PostgreSQL

**Для Windows:**
1. Скачать с https://www.postgresql.org/download/windows/
2. Установить PostgreSQL (версия 14 или новее)
3. При установке:
   - Запомнить пароль для пользователя postgres
   - Порт: 5432 (по умолчанию)

**Проверить установку:**
```powershell
psql --version
```

---

## 🚀 Пошаговый запуск

### ШАГ 1: Установка зависимостей

Откройте PowerShell в папке проекта:

```powershell
# Перейти в папку проекта
cd "C:\Users\Ксения\Yandex.Disk\PythonProjects\web_projects\beauty"

# Создать виртуальное окружение
python -m venv venv

# Активировать виртуальное окружение (Windows)
.\venv\Scripts\activate

# Установить зависимости
pip install -r requirements.txt
```

После этого в начале строки появится `(venv)` - значит окружение активировано ✅

### ШАГ 2: Создание базы данных

Откройте pgAdmin (установился с PostgreSQL) или используйте командную строку:

**Вариант 1: Через pgAdmin**
1. Запустить pgAdmin
2. Ввести пароль postgres
3. Правой кнопкой на "Databases" → "Create" → "Database"
4. Имя: `beauty_db`
5. Сохранить

**Вариант 2: Через командную строку**
```powershell
# Подключиться к PostgreSQL
psql -U postgres

# Создать базу данных (в консоли psql)
CREATE DATABASE beauty_db;

# Выйти
\q
```

### ШАГ 3: Применение схемы базы данных

```powershell
# Применить схему к базе данных
psql -U postgres -d beauty_db -f database/schema.sql
```

Введите пароль postgres → схема применится ✅

### ШАГ 4: Создание .env файла

Создайте файл `.env` в корне проекта:

```powershell
# В папке beauty создать файл .env
notepad .env
```

Вставьте в файл:

```env
# Database
DATABASE_URL=postgresql://postgres:ваш_пароль@localhost:5432/beauty_db

# Security (сгенерировать случайный ключ)
SECRET_KEY=your-secret-key-here-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Telegram (получить у @BotFather - пока можно оставить пустым)
TELEGRAM_BOT_TOKEN=
TELEGRAM_ADMIN_CHAT_ID=

# SMS (пока оставить пустым)
SMS_API_KEY=
SMS_SENDER=Beauty

# Payment (пока оставить пустым)
YOOKASSA_SHOP_ID=
YOOKASSA_SECRET_KEY=

# Application
SITE_URL=http://localhost:8000
ADMIN_EMAIL=admin@example.com
ADMIN_PHONE=+79000000000

# Development
DEBUG=True
ENVIRONMENT=development
```

**ВАЖНО:** Замените `ваш_пароль` на пароль от PostgreSQL!

Сохраните: `Ctrl+S` → закройте Notepad

### ШАГ 5: Запуск Backend сервера

```powershell
# Убедитесь что виртуальное окружение активировано (должно быть (venv) в начале)
# Если нет - запустите: .\venv\Scripts\activate

# Перейти в папку backend
cd backend

# Запустить FastAPI сервер
uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
```

**Вы увидите:**
```
INFO:     Uvicorn running on http://127.0.0.1:8000 (Press CTRL+C to quit)
INFO:     Started reloader process
INFO:     Started server process
INFO:     Waiting for application startup.
INFO:     Application startup complete.
```

**Сервер запущен!** ✅

### ШАГ 6: Открыть сайт в браузере

Откройте браузер и перейдите:

**Главная страница:**
```
http://localhost:8000
```

**API документация (Swagger):**
```
http://localhost:8000/docs
```

**Вы увидите свой сайт!** 🎉

---

## 🎨 Просмотр и редактирование

### Структура файлов для редактирования:

```
beauty/
├── frontend/
│   ├── index.html          ← Главная страница (редактировать)
│   ├── static/
│   │   ├── css/
│   │   │   └── style.css   ← Стили (редактировать)
│   │   └── js/
│   │       └── script.js   ← JavaScript (редактировать)
│   └── templates/          ← HTML шаблоны
└── backend/
    └── app/
        ├── routes/         ← API endpoints
        ├── models/         ← Модели БД
        └── main.py         ← Главный файл

```

### Процесс разработки:

1. **Редактируете HTML/CSS/JS**
   - Открываете файл в VS Code
   - Вносите изменения
   - Сохраняете (Ctrl+S)

2. **Обновляете браузер**
   - Нажимаете F5 в браузере
   - Видите изменения!

3. **Редактируете Python код**
   - Вносите изменения в backend
   - Сохраняете
   - Сервер автоматически перезапускается (--reload)
   - Обновляете браузер

---

## 🧪 Тестирование функций

### Добавление тестовых данных

Можно добавить тестовые услуги и записи:

```powershell
# Подключиться к базе
psql -U postgres -d beauty_db

# Добавить тестовые услуги (уже есть в schema.sql)
SELECT * FROM services;

# Добавить тестового клиента
INSERT INTO clients (name, phone, email)
VALUES ('Тестовый клиент', '+79001234567', 'test@example.com');

# Проверить
SELECT * FROM clients;
```

### Тестирование API

Откройте http://localhost:8000/docs

Там можно:
- Просмотреть все API endpoints
- Протестировать каждый endpoint
- Посмотреть ответы сервера

---

## 🔧 Полезные команды

### Остановить сервер:
```
Ctrl+C в PowerShell
```

### Перезапустить сервер:
```powershell
uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
```

### Просмотр логов:
- Логи видны прямо в PowerShell где запущен сервер

### Очистить базу данных:
```powershell
psql -U postgres -d beauty_db

# Удалить все записи
TRUNCATE appointments, clients, notifications RESTART IDENTITY CASCADE;
```

---

## 📱 Запуск Telegram бота (локально)

Если хотите протестировать Telegram бота:

### 1. Создать бота в Telegram

1. Открыть Telegram
2. Найти @BotFather
3. Отправить `/newbot`
4. Придумать имя: `Beauty Test Bot`
5. Придумать username: `beauty_test_bot` (должен быть уникальным)
6. Получить **TOKEN** (сохранить!)

### 2. Добавить токен в .env

```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
```

### 3. Запустить бота (в новом окне PowerShell)

```powershell
# Активировать виртуальное окружение
cd "C:\Users\Ксения\Yandex.Disk\PythonProjects\web_projects\beauty"
.\venv\Scripts\activate

# Запустить бота
python backend/app/bot/telegram_bot.py
```

**Вы увидите:**
```
🤖 Telegram бот запущен!
```

### 4. Тестировать бота

1. Найти своего бота в Telegram (по username)
2. Отправить `/start`
3. Попробовать `/book` - записаться на прием!

**Всё работает локально, БЕЗ хостинга!** ✅

---

## 🌐 Доступ с телефона (в локальной сети)

Если хотите открыть сайт на телефоне:

### 1. Узнать IP адрес компьютера

```powershell
ipconfig
```

Найти `IPv4 Address` (например: 192.168.1.100)

### 2. Запустить сервер с доступом из сети

```powershell
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 3. Открыть на телефоне

В браузере на телефоне (подключён к той же WiFi):
```
http://192.168.1.100:8000
```

**Сайт откроется на телефоне!** 📱

---

## ❓ Частые проблемы

### Ошибка: "ModuleNotFoundError"
**Решение:** Убедитесь что виртуальное окружение активировано и зависимости установлены
```powershell
.\venv\Scripts\activate
pip install -r requirements.txt
```

### Ошибка: "could not connect to server"
**Решение:** PostgreSQL не запущен
```powershell
# Запустить службу PostgreSQL
net start postgresql-x64-14
```

### Ошибка: "Port 8000 already in use"
**Решение:** Порт занят, используйте другой
```powershell
uvicorn app.main:app --reload --port 8001
```

### Ошибка: "Database does not exist"
**Решение:** Создайте базу данных (см. ШАГ 2)

---

## ✅ Итоговый чеклист

Для локального запуска нужно:

- [ ] Установить Python 3.11+
- [ ] Установить PostgreSQL
- [ ] Создать виртуальное окружение
- [ ] Установить зависимости (pip install)
- [ ] Создать базу данных beauty_db
- [ ] Применить схему (schema.sql)
- [ ] Создать .env файл с настройками
- [ ] Запустить сервер (uvicorn)
- [ ] Открыть в браузере http://localhost:8000

**Всё это БЕСПЛАТНО и БЕЗ хостинга!** 🎉

---

## 🎯 Когда нужен хостинг?

**Хостинг нужен ТОЛЬКО когда:**
- ✅ Разработка закончена
- ✅ Всё протестировано локально
- ✅ Готовы принимать реальных клиентов
- ✅ Хотите чтобы сайт был доступен 24/7 из интернета

**До этого момента - работайте локально бесплатно!**
