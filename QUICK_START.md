# 🚀 Быстрый старт - Запуск проекта локально

Инструкция для запуска Beauty Booking System на вашем компьютере с DBeaver.

## ✅ Что у вас уже есть

- ✅ DBeaver установлен
- ✅ PostgreSQL установлен
- ✅ Python установлен
- ✅ Проект скачан

## 📋 Пошаговый запуск

### ШАГ 1: Создать базу данных в DBeaver

**Через GUI:**
1. Открыть DBeaver
2. Раскрыть подключение PostgreSQL
3. Правой кнопкой на "Databases" → Create New Database
4. Name: `beauty_db`
5. Owner: postgres
6. OK

**Или через SQL:**
```sql
CREATE DATABASE beauty_db WITH ENCODING = 'UTF8';
```

---

### ШАГ 2: Применить схему базы данных

1. В DBeaver правой кнопкой на beauty_db → SQL Editor → Open SQL Script
2. File → Open File → выбрать файл:
   ```
   C:\Users\Ксения\Yandex.Disk\PythonProjects\web_projects\beauty\database\schema.sql
   ```
3. Execute SQL Script (Ctrl+Alt+X)
4. Подтвердить

**Проверить:**
- Раскрыть beauty_db → Schemas → public → Tables
- Должно быть 10 таблиц (clients, services, appointments, и т.д.)

---

### ШАГ 3: Настроить подключение к БД

1. Открыть файл `.env` в корне проекта
2. Изменить пароль PostgreSQL:

```env
DATABASE_URL=postgresql://postgres:ВАШ_ПАРОЛЬ@localhost:5432/beauty_db
```

**Замените `ВАШ_ПАРОЛЬ` на ваш пароль от PostgreSQL!**

---

### ШАГ 4: Установить зависимости Python

Открыть PowerShell в папке проекта:

```powershell
# Перейти в папку проекта
cd "C:\Users\Ксения\Yandex.Disk\PythonProjects\web_projects\beauty"

# Создать виртуальное окружение
python -m venv venv

# Активировать (на Windows)
.\venv\Scripts\activate

# Должно появиться (venv) в начале строки

# Установить зависимости
pip install -r requirements.txt
```

**Ожидайте:** Установка займёт 1-2 минуты

---

### ШАГ 5: Запустить Backend сервер

```powershell
# Убедитесь что (venv) активировано
# Если нет - запустите: .\venv\Scripts\activate

# Перейти в папку backend
cd backend

# Запустить FastAPI сервер
python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
```

**Вы увидите:**
```
INFO:     Uvicorn running on http://127.0.0.1:8000 (Press CTRL+C to quit)
INFO:     Started reloader process
INFO:     Application startup complete.
```

**✅ Сервер запущен!**

---

### ШАГ 6: Открыть сайт в браузере

**Главная страница:**
```
http://localhost:8000
```

**API документация (Swagger):**
```
http://localhost:8000/docs
```

**Интерактивная документация (ReDoc):**
```
http://localhost:8000/redoc
```

---

## 🧪 Проверка работы

### Проверить API endpoints:

Откройте http://localhost:8000/docs

**Попробуйте:**
1. GET /api/services - получить список услуг
   - Нажать "Try it out" → "Execute"
   - Должны увидеть 5 услуг

2. GET /api/appointments/available-slots - проверить свободные слоты
   - Указать дату (например: 2026-02-05)
   - Execute
   - Должны увидеть доступное время

### Проверить БД через DBeaver:

```sql
-- Посмотреть услуги
SELECT * FROM services;

-- Посмотреть расписание
SELECT * FROM work_schedule;

-- Посмотреть записи (пока пусто)
SELECT * FROM appointments;
```

---

## 🎨 Разработка и изменения

### Структура файлов:

```
beauty/
├── frontend/
│   ├── index.html          ← Главная страница (редактировать)
│   └── static/
│       ├── css/
│       │   └── style.css   ← Стили
│       └── js/
│           └── script.js   ← JavaScript
├── backend/
│   └── app/
│       ├── main.py         ← Главный файл FastAPI
│       ├── routes/         ← API endpoints
│       ├── models/         ← Модели БД
│       └── services/       ← Бизнес-логика
└── database/
    └── schema.sql          ← Схема БД
```

### Процесс разработки:

1. **Редактируете файлы** в VS Code
2. **Сохраняете** (Ctrl+S)
3. **Обновляете браузер** (F5)
4. **Видите изменения!**

Сервер автоматически перезагружается при изменении Python файлов (флаг `--reload`)!

---

## 📱 Запуск Telegram бота (опционально)

Если хотите протестировать Telegram бота локально:

### 1. Создать бота:
1. Telegram → найти @BotFather
2. Отправить `/newbot`
3. Придумать имя и username
4. Получить **ТОКЕН**

### 2. Добавить токен в .env:
```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
TELEGRAM_ADMIN_CHAT_ID=ваш_telegram_id
```

Узнать свой ID: отправить /start боту @userinfobot

### 3. Запустить бота (в новом окне PowerShell):
```powershell
cd "C:\Users\Ксения\Yandex.Disk\PythonProjects\web_projects\beauty"
.\venv\Scripts\activate
python backend/app/bot/telegram_bot.py
```

**Вы увидите:**
```
🤖 Telegram бот запущен!
```

### 4. Тестировать:
- Найти бота в Telegram
- Отправить `/start`
- Попробовать `/book` - записаться на прием!

**Всё работает локально с вашей БД в DBeaver!** ✅

---

## 🌐 Доступ с телефона (в локальной сети)

Если хотите открыть сайт на телефоне:

### 1. Узнать IP компьютера:
```powershell
ipconfig
```
Найти IPv4 Address (например: 192.168.1.100)

### 2. Запустить сервер для сети:
```powershell
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 3. Открыть на телефоне:
В браузере (телефон в той же WiFi):
```
http://192.168.1.100:8000
```

---

## 🛑 Остановка сервера

**В PowerShell где запущен сервер:**
```
Ctrl+C
```

---

## 🔄 Перезапуск после изменений

### Изменения в Python коде:
- Сервер автоматически перезагрузится (флаг `--reload`)
- Просто обновите браузер

### Изменения в HTML/CSS/JS:
- Просто обновите браузер (F5)

### Изменения в схеме БД:
```sql
-- В DBeaver выполнить новые команды ALTER TABLE
ALTER TABLE clients ADD COLUMN new_field VARCHAR(100);
```

---

## 📊 Добавление тестовых данных

### Через DBeaver (GUI):
1. Раскрыть Tables → clients
2. Правой кнопкой → View Data
3. Внизу панели нажать "Add Row" (+)
4. Заполнить данные
5. Сохранить (Ctrl+S)

### Через SQL:
```sql
-- Добавить тестового клиента
INSERT INTO clients (name, phone, email)
VALUES ('Анна Иванова', '+79001234567', 'anna@example.com');

-- Добавить запись на прием
INSERT INTO appointments (client_id, service_id, appointment_date, appointment_time, status, duration_minutes, total_price)
VALUES (1, 1, '2026-02-05', '14:00', 'pending', 60, 3500.00);

-- Проверить
SELECT
    c.name as client,
    s.name as service,
    a.appointment_date,
    a.appointment_time,
    a.total_price
FROM appointments a
JOIN clients c ON a.client_id = c.id
JOIN services s ON a.service_id = s.id;
```

---

## ✅ Итоговый чеклист первого запуска

- [ ] DBeaver открыт, PostgreSQL работает
- [ ] База данных beauty_db создана
- [ ] Схема применена (10 таблиц видны)
- [ ] .env файл настроен (правильный пароль БД)
- [ ] Виртуальное окружение создано (venv)
- [ ] Зависимости установлены (pip install)
- [ ] Сервер запущен (uvicorn)
- [ ] Сайт открывается http://localhost:8000
- [ ] API docs работают http://localhost:8000/docs

---

## 🎯 Что дальше?

### Сейчас (разработка):
1. ✅ Локальная БД в DBeaver работает
2. ✅ Сервер запускается локально
3. ✅ Можно разрабатывать и тестировать
4. ✅ **Стоимость: 0 ₽**

### Потом (запуск в интернет):
1. Завершить разработку и тестирование
2. Арендовать VPS (180 ₽/мес)
3. Создать БД на сервере
4. Применить ту же схему (schema.sql)
5. Загрузить код
6. Запустить для клиентов

**Всё локально → потом в интернет!** ✅

---

## ❓ Частые проблемы

### "ModuleNotFoundError: No module named 'fastapi'"
**Решение:** Активировать venv и установить зависимости
```powershell
.\venv\Scripts\activate
pip install -r requirements.txt
```

### "could not connect to server"
**Решение:** PostgreSQL не запущен
- Открыть DBeaver (автоматически запустит PostgreSQL)
- Или: Services → PostgreSQL → Start

### "FATAL: database does not exist"
**Решение:** Создать БД beauty_db в DBeaver (см. ШАГ 1)

### "Port 8000 already in use"
**Решение:** Использовать другой порт
```powershell
uvicorn app.main:app --reload --port 8001
```

### "Authentication failed"
**Решение:** Неправильный пароль в .env
- Проверить пароль PostgreSQL
- Изменить в .env файле

---

## 📞 Поддержка

Если что-то не работает:

1. Проверить что PostgreSQL запущен (открыть DBeaver)
2. Проверить что БД beauty_db существует
3. Проверить .env файл (правильный пароль)
4. Проверить что venv активировано (должно быть (venv) в PowerShell)
5. Посмотреть логи сервера в PowerShell

---

**Готово! Успешного тестирования!** 🚀
